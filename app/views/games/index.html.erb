<div class="col-12">
  <div class="card">
    <div class="card-header">
      <div class="float-left">
        CL Logo
      </div>
      <div class="float-right">
        Quit
      </div>
    </div>
    <div class="card-block card-container">
      <div class="row">
        <%# TODO load values that maybe already happened %>
        <% @game.each do |g| %>
          <div class="col-md-4 game-piece">
            <h3><%= g[:name] %></h3>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>